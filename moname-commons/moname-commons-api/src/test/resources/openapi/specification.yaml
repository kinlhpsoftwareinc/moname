# https://spec.openapis.org/oas/latest.html
# https://json-schema.org/specification.html
#openapi: 3.1.0
openapi: 3.0.3
info:
  title: moname-commons-api
  #summary: *info-title
  description: Project to money me with no name [commons-api].
  termsOfService: https://moname.kinlhp.com/api/terms
  contact:
    name: Kin.LHP® Software, Inc.
    url: https://kinlhp.com
    email: lhp.kin@gmail.com
  license:
    name: MIT License
    #identifier: MIT
    url: https://www.opensource.org/licenses/mit-license.php
  version: 1.0.0.BUILD-SNAPSHOT
#jsonSchemaDialect: https://spec.openapis.org/oas/3.1/dialect/base
servers:
  - url: http://{host}:{port}/{basePath}
    description: The local development API server.
    variables:
      host:
        # NOTE! No enum here means it is an open value.
        enum:
          - localhost
        default: localhost
        description:
      port:
        # NOTE! No enum here means it is an open value.
        enum:
          - 8080
        default: 8080
        description:
      basePath:
        # NOTE! No enum here means it is an open value.
        enum:
          - api
        default: api
        description:
  - url: https://{host}:{port}
    description: The production API server.
    variables:
      host:
        # NOTE! No enum here means it is an open value.
        enum:
          - api.moname.kinlhp.com
        default: api.moname.kinlhp.com
        description: This value is assigned by the service provider.
      port:
        # NOTE! No enum here means it is an open value.
        enum:
          - 8080
        default: 8080
        description: This value is assigned by the service provider.
paths:
  /v1/entities:
    #
    # Escaped forward-slash is necessary when using JSON references.
    # https://spec.openapis.org/oas/latest.html#operationref-examples
    $ref: 'paths/v1/entities.yaml#/v1~1entities'
#webhooks:
#components:
  #schemas:
  #responses:
  #parameters:
  #examples:
  #requestBodies:
  #headers:
  #securitySchemes:
  #links:
  #callbacks:
  #pathItems:
#security:
tags:
  - name: entity
    description:
    #externalDocs:
#externalDocs:
