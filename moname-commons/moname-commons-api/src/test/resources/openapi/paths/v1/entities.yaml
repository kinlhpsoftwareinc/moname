# https://spec.openapis.org/oas/latest.html
# https://json-schema.org/specification.html
v1/entities:
  summary:
  description:
  get:
    tags:
      - entity
    summary: &paths-v1-entities-get-summary
    description: *paths-v1-entities-get-summary
    #externalDocs:
    operationId: getEntities
    parameters:
      - $ref: '../../components/parameters.yaml#/pageIndex'
      - $ref: '../../components/parameters.yaml#/pageSize'
    #requestBody:
    responses:
      200:
        $ref: '../../components/responses.yaml#/ok'
      default:
        $ref: '../../components/responses.yaml#/internalServerError'
    #callbacks:
    deprecated: false
    #security:
    #servers:
  #servers:
  #parameters:
